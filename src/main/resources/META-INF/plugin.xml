<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>com.czwei.SvnMavenBuild</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>svn-maven-build</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor email="1043351876@qq.com">czwei</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Simple HTML elements (text formatting, paragraphs, and lists) can be added inside of <![CDATA[ ]]> tag.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
        <p>Generate incremental WAR/Class update packages based on SVN commit history using Maven. Supports both WAR mode (mvn package then extract) and Classes mode (copy directly from target/classes). Inner classes (Foo$Bar.class) are automatically included.</p>
        <p>基于 SVN 提交记录，使用 Maven 增量打包 WAR/Class 更新包。支持 WAR 模式和 Classes 模式，自动包含内部类文件。</p>
        <br>
        <h3>Requirements / 依赖</h3>
        <ul>
            <li>SVN must be installed and configured in PATH</li>
            <li>Maven must be installed and configured in PATH</li>
        </ul>
        <h3>Usage / 使用方法</h3>
        <ul>
            <li>Right-click project → Subversion → Show History → select revisions → Svn Maven build</li>
            <li>Select files and packaging mode (WAR / Classes) in the dialog, then click OK</li>
            <li>The update package will be generated under target/dist/</li>
        </ul>
  ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="Svn Maven Build" displayType="BALLOON" key="notification.group.name"/>
    </extensions>

    <!-- action配置，按钮展示在哪里需要在这配置 -->
    <actions>
        <action class="com.example.subversionMavenIncrement.ChoiceAction" icon="AllIcons.General.CopyHovered" id="SvnMavenBuild" text="Svn Maven build" description="根据svn提交记录,使用maven打包更新包" >
            <!-- 将插件放在History菜单中的first位置。 -->
            <add-to-group group-id="VcsHistoryActionsGroup" anchor="first" />
        </action>
    </actions>

</idea-plugin>